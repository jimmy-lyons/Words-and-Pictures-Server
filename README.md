# Words-and-Pictures-Server

## Setup

- Run: <br>
  if your system doesn't default to python3:
  ```
  alias python=python3
  ```
  if you need to set up a python virtual environment:
  ```
  python -m venv .
  ```